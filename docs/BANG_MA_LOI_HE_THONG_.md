# BẢNG DANH MỤC MÃ LỖI VÀ CẢNH BÁO HỆ THỐNG TRẠM SẠC (CHARGER SYSTEM FAULT SPECIFICATION)
  
## BẢNG TRA CỨU MÃ LỖI HỆ THỐNG CHI TIẾT

### Quy ước severity BMS

Trong `ALM_INFO`: severity `0` là không cảnh báo, `1` là warning chỉ để hiển thị/log, `2..3` là fault/severe có thể kích hoạt hành vi an toàn. Severity 1 vẫn dùng mã `AlarmCode_t` hiện có nhưng action luôn là `INFO`; chỉ severity từ 2 trở lên mới được phép STOP/ESTOP. Các dòng mô tả `severity >= 2` bên dưới là điều kiện safety, không phải điều kiện duy nhất để reporting. Ở firmware, `ev_bms()` kiểm tra cả `warning_flags` và `alarm_flags`; các quyết định an toàn chỉ đọc `alarm_flags`.

### Quy ước nguồn gốc alarm

- **CAN**: BMS/module tự phát hiện, gửi raw field qua CAN; firmware parse và mirror.
- **Connection-derived**: chỉ áp dụng cho mất kết nối do timeout (`W010`, `E021`).
- **Controller-derived**: quyết định an toàn từ telemetry CAN hợp lệ (`E022`).
- **Station-policy-derived**: chính sách cấp trạm dựa trên alarm CAN của module (`E026` từ `W011`).
- **Data-status**: `BMS_IsDataStale()` là chất lượng dữ liệu nội bộ, không phải alarm và không tạo DWIN code.
- **DWIN khi IDLE/chưa có CAN**: controller giữ trạng thái `IDLE/READY`, không tạo alarm chỉ vì BMS/module chưa online. Các trường đo lường chưa có dữ liệu (DC, AC, nhiệt độ và BMS) hiển thị dạng Text Display `---`; SOC offline hiển thị `--%`, SOC online hiển thị dạng text, ví dụ `50%`. `0` không được dùng để biểu diễn dữ liệu chưa tồn tại.
- **Màu SOC trên DWIN**: SOC Text Display dùng SP `0x8000`; Text Color nằm tại `0x8003` (offset +3 WORD). RGB565: unavailable `0x8410`, critical `0xF800` (0–10%), low `0xFD20` (11–30%), medium `0xD520` (31–60%, amber dịu), normal `0x2CEA` (61–100%, xanh lá dịu). Màu chỉ phục vụ hiển thị, không tạo alarm.

| Lỗi từ | Lỗi | Mã lỗi | Hiển thị | Hành vi | Nguyên nhân | Logic phát hiện trong Code (Không suy diễn) |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **Module/TonHe** | Mất pha AC đầu vào module | **E025** | Mất pha AC đầu vào module | **Dừng sạc an toàn (STOP)** | Chỉ xác nhận từ các trường phase-loss trong PDF TonHe. | **PDF → raw → normalized → alarm → DWIN → action**: TonHe `M_C_1` bit 1 hoặc PFC byte bit 6 → `CHG_LIB_ALARM_AC_PHASE_LOSS` → `ALARM_AC_PHASE_LOSS` → `E025` → STOP; debounce/latching theo alarm.c (1000 ms, latched). Maxwell bit 4/5 không phải phase loss. **Đã triển khai.** |
| **Module** | Quá áp AC đầu vào module | **E017** | Quá áp AC đầu vào module | **Dừng sạc an toàn (Stop Charge)**<br>Hạ lệnh dòng/áp về 0, ngắt rơ-le DC. Khóa module chờ điện áp lưới ổn định. | Điện áp lưới điện AC 3 pha vượt quá ngưỡng bảo vệ tối đa của module (thường > 456 VAC đối với lưới 380V hoặc sét lan truyền / cắt phụ tải lớn). | **Module tự phát hiện và gửi qua CAN (Mirror 1:1)**:<br>- **TonHe**: Frame `M_C_1` Byte 6 bit 2.<br>- **Maxwell**: Register `0x0040` bit 9 (`AC_OV`).<br>- **Lianming**: Status Word Byte 7 bit 4.<br>- **alarm.c**: Kiểm tra trực tiếp cờ `(in->mod_alarm_or & CHG_LIB_ALARM_AC_OVER_VOLT) != 0`. |
| **Module** | Thấp áp AC đầu vào module | **W011** | Điện áp AC vào module thấp | **Cảnh báo (Warning) & Giảm công suất**<br>Module tự động giảm dòng sạc (derating). Nếu sụt áp quá sâu (< 260 VAC), trạm sẽ dừng sạc. | Điện áp lưới AC 3 pha sụt xuống dưới ngưỡng danh định cho phép (thường < 304 VAC - 260 VAC) do quá tải đường dây cấp nguồn hoặc trạm biến áp quá tải. | **Module tự phát hiện và gửi qua CAN (Mirror 1:1)**:<br>- **TonHe**: Frame `M_C_1` Byte 6 bit 0.<br>- **Maxwell**: Register `0x0040` bit 14 (`AC_UNDERVOLTAGE`).<br>- **Lianming**: Status Word Byte 7 bit 5.<br>- **alarm.c**: Kiểm tra trực tiếp cờ `(in->mod_alarm_or & CHG_LIB_ALARM_AC_UNDER_VOLT) != 0`. |
| **Module** | Thấp áp đầu ra DC (warning) | **W012** | Cảnh báo thấp áp đầu ra module | **INFO** | TonHe `M_C_1` bit 12 hoặc Lianming Byte 7 bit 7 theo PDF. | raw bit/byte → `CHG_LIB_ALARM_OUTPUT_UNDER_VOLT` → `ALARM_MOD_OUTPUT_UNDER_VOLT` → `W012` → INFO; debounce mirror, không latching nâng cấp. **Đã triển khai.** |
| **Module** | Quá áp đầu ra DC (warning) | **W013** | Cảnh báo quá áp đầu ra module | **INFO** | TonHe `M_C_1` bit 13 theo PDF. | raw bit/byte → `CHG_LIB_ALARM_OUTPUT_OVER_VOLT_WARN` → `ALARM_MOD_OUTPUT_OVER_VOLT_WARN` → `W013` → INFO; debounce mirror, không latching nâng cấp. **Đã triển khai.** |
| **Module** | Quá áp đầu ra DC module (protection) | **E012** | Quá áp đầu ra module sạc | **Dừng khẩn cấp (E-STOP)** | Chỉ dùng các bit output-overvoltage protection theo PDF. | Maxwell register `0x0040` bit 7/31 hoặc TonHe `M_C_1` bit 3 hoặc Lianming Byte 7 bit 6 → `CHG_LIB_ALARM_OVER_VOLTAGE_OUT` → `ALARM_MOD_OVER_VOLT_OUT` → `E012` → E-STOP; mirror debounce. TonHe bit 13 là W013, không phải E012. **Đã triển khai.** |
| **Module** | Quá dòng đầu ra DC module | **E014** | Quá dòng đầu ra module sạc | **Dừng sạc an toàn (Stop Charge)**<br>Hạ lệnh dòng về 0, ngắt rơ-le DC. | Dòng điện thực tế ngõ ra của module vượt quá ngưỡng dòng điện định mức tối đa (I > 110% Imax) do tải pin kéo dòng bất thường hoặc cảm biến Hall của module sai lệch. | **Module tự phát hiện và gửi qua CAN (Mirror 1:1)**:<br>- **TonHe**: Frame `M_C_1` Byte 6 bit 4.<br>- **Maxwell**: Register `0x0040` bit 17.<br>- **Lianming**: Status Word Byte 6 bit 0.<br>- **alarm.c**: Kiểm tra cờ `(in->mod_alarm_or & CHG_LIB_ALARM_OVER_CURR_OUT) != 0`. |
| **Module** | Ngắn mạch đầu ra module | **E013** | Ngắn mạch đầu ra module | **Dừng khẩn cấp (E-STOP)** | Chỉ các nguồn có trường short-circuit trong PDF. | Maxwell register `0x0040` bit 28 hoặc TonHe `M_C_1` bit 15 → `CHG_LIB_ALARM_SHORT_CIRCUIT` → `ALARM_MOD_SHORT_CIRCUIT` → `E013` → E-STOP; mirror debounce. Lianming không có E013. **Đã triển khai.** |
| **Module** | Quá nhiệt module sạc | **E011** | Quá nhiệt module sạc | **Dừng sạc an toàn (Stop Charge)**<br>Ngắt sạc, duy trì quạt tản nhiệt ở tốc độ tối đa để hạ nhiệt khối công suất. Tự phục hồi khi nhiệt độ về ngưỡng an toàn. | Nhiệt độ khối tản nhiệt (Heatsink) hoặc biến áp xung DC-DC bên trong module vượt quá ngưỡng cho phép (thường > 85°C - 90°C). | **Module tự phát hiện và gửi qua CAN (Mirror 1:1)**:<br>- **TonHe**: Frame `M_C_1` Byte 6 bit 5 & `M_C_4` bit 6, 7, 9, 12.<br>- **Maxwell**: Register `0x0040` bit 24 & bit 30 (`DCDC_OVERTEMP`).<br>- **Lianming**: Status Word Byte 6 bit 1.<br>- **alarm.c**: Kiểm tra trực tiếp cờ `(in->mod_alarm_or & CHG_LIB_ALARM_OVER_TEMP) != 0`. |
| **Module** | Lỗi quạt làm mát module | **E016** | Lỗi quạt module sạc | **Dừng sạc an toàn (Stop Charge)**<br>Dừng sạc ngay để ngăn chặn module bị sốc nhiệt và phá hủy linh kiện công suất. | Một hoặc nhiều quạt làm mát bên trong module sạc bị kẹt cơ học, đứt dây tín hiệu hồi tiếp tốc độ (Tachometer/PWM), hoặc quạt bị cháy cuộn dây. | **Module tự phát hiện và gửi qua CAN (Mirror 1:1)**:<br>- **TonHe**: Frame `M_C_1` Byte 6 bit 6 (`Fan fault`).<br>- **Maxwell**: Register `0x0040` bit 27 (`FAN_FAILURE`).<br>- **Lianming**: Status Word Byte 7 bit 3.<br>- **alarm.c**: Kiểm tra trực tiếp cờ `(in->mod_alarm_or & CHG_LIB_ALARM_FAN_FAULT) != 0`. |
| **Module** | Lỗi mạch PFC module | **E015** | Lỗi mạch PFC module sạc | **Dừng sạc an toàn (STOP)** | Chỉ các PFC field được nêu trong PDF. | Maxwell register `0x0040` bit 8 hoặc TonHe PFC byte bit 0–7 khác 0 → `CHG_LIB_ALARM_PFC_FAULT`/`pfc_fault` → `ALARM_MOD_PFC_FAULT` → `E015` → STOP; mirror debounce. Lianming không có E015. **Đã triển khai.** |
| **Module** | Lỗi phần cứng module | **E010** | Lỗi phần cứng module sạc | **Dừng sạc an toàn (Stop Charge)**<br>Đánh dấu module bị lỗi, loại trừ module khỏi nhóm phát công suất hoặc dừng sạc trạm. | DSP/MCU nội bộ module tự phát hiện hỏng hóc phần cứng: Hỏng nguồn nuôi phụ cấp (+15V, +5V, -15V), rơ-le xả mềm bị dính/hỏng, lỗi cảm biến, lỗi bộ nhớ EEPROM. | **Module tự phát hiện và gửi qua CAN (Mirror 1:1)**:<br>- **TonHe**: Frame `M_C_1` Byte 6 bit 7 (`Hardware fault`).<br>- **Maxwell**: Register `0x0040` bit 0 (`MODULE_FAULT`) hoặc bit 1.<br>- **Lianming**: Status Word Byte 7 bit 1.<br>- **alarm.c**: Kiểm tra trực tiếp cờ `(in->mod_alarm_or & CHG_LIB_ALARM_HW_FAULT) != 0`. |
| **Module** | Mất giao tiếp module sạc | **W010** | Mất giao tiếp module sạc | **Cảnh báo (Warning)** nếu chỉ mất 1 vài module trong cụm (tiếp tục sạc với công suất giảm); **Dừng sạc** nếu mất kết nối toàn bộ module. | Cáp CAN bus nối giữa Bo điều khiển chính và các module sạc bị đứt, lỏng giắc cắm, chập dây CANH/CANL, lỗi trở đầu cuối 120Ω, hoặc module mất nguồn nuôi. | **Bo điều khiển trạm tự phát hiện qua Watchdog Timer**:<br>- `chg_lib` đếm thời gian: `now - module[i].last_rx_tick > 2000ms`.<br>- Nếu quá 2000ms không nhận được bất kỳ frame phản hồi nào từ module, trạm tự gán `online = false` và bật cờ `CHG_LIB_ALARM_COMM_FAIL`. |
| **BMS** | Quá áp pack pin BMS | **E003** | Điện áp pack BMS cao | **Dừng sạc an toàn (Stop Charge)**<br>Hạ dòng sạc về 0 ngay lập tức, ngắt rơ-le DC sạc để bảo vệ bộ pin không bị quá tải điện áp. | Tổng điện áp của bộ pin xe (Pack Voltage) đo được vượt quá ngưỡng điện áp sạc tối đa do BMS xe quy định. | **BMS xe tự phát hiện & gửi qua CAN (Mirror 1:1)**:<br>- Nhận bản tin `ALM_INFO` (`0x07F4`), trường `high_pack_volt` với `severity >= 2`.<br>- `alarm.c`: `ev_bms()` kiểm tra trực tiếp `(in->bms.alarm_flags & BMS_ALARM_HIGH_PACK_VOLT) != 0`. |
| **BMS** | Điện áp pack pin thấp | **E001** | Điện áp pack BMS thấp | **Cảnh báo (Warning / Info)**<br>Ghi nhận trạng thái pin cạn. Nếu điện áp dưới ngưỡng an toàn tối thiểu, hệ thống sẽ từ chối kích hoạt sạc. | Tổng điện áp toàn bộ pack pin quá thấp do xe bị xả cạn kiệt trong thời gian dài, hoặc có cell pin bên trong bị tụt áp sâu / hỏng hóc. | **BMS xe tự phát hiện & gửi qua CAN (Mirror 1:1)**:<br>- Nhận bản tin `ALM_INFO` (`0x07F4`), trường `low_pack_volt` với `severity >= 2`.<br>- `alarm.c`: `ev_bms()` kiểm tra trực tiếp `(in->bms.alarm_flags & BMS_ALARM_LOW_PACK_VOLT) != 0`. |
| **BMS** | Quá áp cell pin BMS | **E004** | Quá áp cell pin BMS | **Dừng sạc an toàn (Stop Charge)**<br>Ngắt sạc lập tức để bảo vệ cell pin không bị phồng, cháy nổ. | Có ít nhất một cell đơn lẻ trong pack đạt ngưỡng điện áp bảo vệ cắt sạc (thường > 3.65V với LFP, > 4.25V với NMC), pin bị mất cân bằng trầm trọng. | **BMS xe tự phát hiện & gửi qua CAN (Mirror 1:1)**:<br>- Nhận bản tin `ALM_INFO` (`0x07F4`), trường `high_cell_volt` với `severity >= 2`.<br>- `alarm.c`: `ev_bms()` kiểm tra trực tiếp `(in->bms.alarm_flags & BMS_ALARM_HIGH_CELL_VOLT) != 0`. |
| **BMS** | Điện áp cell pin thấp | **E002** | Điện áp cell BMS thấp | **Cảnh báo (Warning / Info)**<br>Hiển thị mức cell thấp, cảnh báo người dùng. Trạm sạc giới hạn dòng sạc nhỏ (Pre-charge / Trickle charge) để phục hồi cell. | Cell pin bị phóng điện quá sâu (thường < 2.5V đối với LFP, < 3.0V đối với NMC). | **BMS xe tự phát hiện & gửi qua CAN (Mirror 1:1)**:<br>- Nhận bản tin `ALM_INFO` (`0x07F4`), trường `low_cell_volt` với `severity >= 2`.<br>- `alarm.c`: `ev_bms()` kiểm tra trực tiếp `(in->bms.alarm_flags & BMS_ALARM_LOW_CELL_VOLT) != 0`. |
| **BMS** | Quá nhiệt khi sạc BMS | **E005** | Quá nhiệt khi sạc BMS | **Dừng sạc an toàn (Stop Charge)**<br>Ngắt dòng sạc ngay lập tức, không cho phép tiếp tục sạc cho đến khi nhiệt độ pin giảm về mức an toàn. | Nhiệt độ tại một hoặc nhiều cảm biến nhiệt độ cell pin bên trong pack vượt quá ngưỡng cho phép khi nạp (thường > 55°C - 60°C). | **BMS xe tự phát hiện & gửi qua CAN (Mirror 1:1)**:<br>- Nhận bản tin `ALM_INFO` (`0x07F4`), trường `temp_cell_high_chg` với `severity >= 2`.<br>- `alarm.c`: `ev_bms()` kiểm tra trực tiếp `(in->bms.alarm_flags & BMS_ALARM_TEMP_HIGH_CHG) != 0`. |
| **BMS** | Nhiệt độ sạc BMS thấp | **E006** | Nhiệt độ sạc BMS thấp | **Dừng sạc an toàn (Stop Charge)**<br>Từ chối hoặc ngắt sạc để tránh hiện tượng mạ lithium (lithium plating) làm hỏng cell pin vĩnh viễn. | Nhiệt độ cell pin quá lạnh (dưới 0°C đối với pin Lithium-ion/LFP tiêu chuẩn khi sạc). | **BMS xe tự phát hiện & gửi qua CAN (Mirror 1:1)**:<br>- Nhận bản tin `ALM_INFO` (`0x07F4`), trường `temp_cell_low_chg` với `severity >= 2`.<br>- `alarm.c`: `ev_bms()` kiểm tra trực tiếp `(in->bms.alarm_flags & BMS_ALARM_TEMP_LOW_CHG) != 0`. |
| **BMS** | Quá nhiệt khi xả BMS | **W001** | Quá nhiệt khi xả BMS | **Cảnh báo (Warning / Info)**<br>Cảnh báo hiển thị trên giao diện giám sát để theo dõi trạng thái pin xe. | BMS gửi cờ cảnh báo nhiệt độ trong quá trình xả điện trên xe vượt ngưỡng cảnh báo (áp dụng cho trạng thái phát điện V2L/V2G hoặc cấp tải phụ). | **BMS xe tự phát hiện & gửi qua CAN (Mirror 1:1)**:<br>- Nhận bản tin `ALM_INFO` (`0x07F4`), trường `temp_cell_high_dchg`.<br>- `alarm.c`: `ev_bms()` kiểm tra trực tiếp `(in->bms.alarm_flags & BMS_ALARM_TEMP_HIGH_DCHG) != 0`. |
| **BMS** | Nhiệt độ xả BMS thấp | **W002** | Nhiệt độ xả BMS thấp | **Cảnh báo (Warning / Info)**<br>Cảnh báo trạng thái vận hành nhiệt độ thấp của bộ pin. | Nhiệt độ môi trường làm việc của pack pin dưới ngưỡng xả tối ưu. | **BMS xe tự phát hiện & gửi qua CAN (Mirror 1:1)**:<br>- Nhận bản tin `ALM_INFO` (`0x07F4`), trường `temp_cell_low_dchg`.<br>- `alarm.c`: `ev_bms()` kiểm tra trực tiếp `(in->bms.alarm_flags & BMS_ALARM_TEMP_LOW_DCHG) != 0`. |
| **BMS** | Quá nhiệt rơ-le BMS | **W003** | Quá nhiệt rơ le BMS | **Cảnh báo (Warning)**<br>Cảnh báo tiếp điểm rơ-le xe đang quá nóng. Nếu tiếp tục tăng cao, trạm sẽ chủ động dừng sạc. | Cảm biến nhiệt độ gắn tại tiếp điểm rơ-le/contactor chính của bộ pin xe báo nhiệt độ tăng cao bất thường do tiếp xúc lỏng hoặc dòng sạc lớn kéo dài. | **BMS xe tự phát hiện & gửi qua CAN (Mirror 1:1)**:<br>- Nhận bản tin `ALM_INFO` (`0x07F4`), trường `temp_relay_high`.<br>- `alarm.c`: `ev_bms()` kiểm tra trực tiếp `(in->bms.alarm_flags & BMS_ALARM_TEMP_RELAY_HIGH) != 0`. |
| **BMS** | Quá dòng sạc BMS | **E007** | Quá dòng sạc BMS | **Dừng sạc an toàn (Stop Charge)**<br>Ngắt sạc lập tức để bảo vệ bộ pin và đường cáp kết nối. | Dòng sạc thực tế đo được từ BMS vượt quá giới hạn dòng sạc cho phép tối đa của bộ pin (I_chg > I_max_charge_allow). | **BMS xe tự phát hiện & gửi qua CAN (Mirror 1:1)**:<br>- Nhận bản tin `ALM_INFO` (`0x07F4`), trường `over_chg_curr` với `severity >= 2`.<br>- `alarm.c`: `ev_bms()` kiểm tra trực tiếp `(in->bms.alarm_flags & BMS_ALARM_OVER_CHG_CURR) != 0`. |
| **BMS** | Quá dòng xả BMS | **W004** | Quá dòng xả BMS | **Cảnh báo (Warning / Info)**<br>Ghi nhận cờ cảnh báo dòng xả từ BMS. | Tải tiêu thụ xả từ pin xe vượt quá mức định mức an toàn của nhà sản xuất pin. | **BMS xe tự phát hiện & gửi qua CAN (Mirror 1:1)**:<br>- Nhận bản tin `ALM_INFO` (`0x07F4`), trường `over_dchg_curr`.<br>- `alarm.c`: `ev_bms()` kiểm tra trực tiếp `(in->bms.alarm_flags & BMS_ALARM_OVER_DCHG_CURR) != 0`. |
| **BMS** | Lệch điện áp cell BMS | **W005** | Lệch điện áp cell BMS | **Cảnh báo (Warning)**<br>Cảnh báo hiển thị trên màn hình. Tự động giảm dòng sạc để BMS trên xe kích hoạt cân bằng thụ động/chủ động. | Độ chênh lệch điện áp giữa cell cao nhất (Vmax) và cell thấp nhất (Vmin) trong pack vượt quá ngưỡng cho phép (Delta V > 100mV - 300mV). | **BMS xe tự phát hiện & gửi qua CAN (Mirror 1:1)**:<br>- Nhận bản tin `ALM_INFO` (`0x07F4`), trường `cell_volt_diff`.<br>- `alarm.c`: `ev_bms()` kiểm tra trực tiếp `(in->bms.alarm_flags & BMS_ALARM_CELL_VOLT_DIFF) != 0`. |
| **BMS** | Mức pin BMS thấp | **W006** | Mức pin BMS thấp | **Cảnh báo (Warning / Info)**<br>Hiển thị icon pin yếu, thông báo mức năng lượng còn lại rất thấp. | Dung lượng pin xe (State of Charge - SOC) còn dưới mức cảnh báo (SOC < 10% hoặc SOC < 5%). | **BMS xe tự phát hiện & gửi qua CAN (Mirror 1:1)**:<br>- Nhận bản tin `ALM_INFO` (`0x07F4`), trường `low_soc`.<br>- `alarm.c`: `ev_bms()` kiểm tra trực tiếp `(in->bms.alarm_flags & BMS_ALARM_LOW_SOC) != 0`. |
| **BMS** | Mất kết nối BMS khi sạc | **E021** | Mất kết nối BMS khi sạc | **Dừng sạc an toàn (Stop Charge)**<br>Ngắt ngay lệnh điều khiển công suất, mở contactor DC sau khi dòng sụt về 0. | Trạm sạc không nhận được bản tin định kỳ CAN từ BMS xe trong quá trình đang sạc vượt quá thời gian timeout (thời gian ngắt kết nối > 2000ms - 5000ms). Cáp CAN bị tuột hoặc BMS xe bị treo/mất nguồn. | **Bo điều khiển trạm tự phát hiện qua Watchdog Timer**:<br>- `bms_core.c` đếm: `now - last_rx_tick > BMS_OFFLINE_TIMEOUT_MS = 5000ms`.<br>- Quá 5s không nhận được frame nào từ BMS: Trạm gán `bms.online = false` -> `ev_bms_comm_lost()` kích hoạt `ALARM_BMS_COMM_LOST`. |
| **BMS** | Không có điện áp pin | **E022** | Không có điện áp pin | **Dừng sạc an toàn (Stop Charge)**<br>Không đóng contactor sạc, từ chối khởi động chu trình. | BMS báo trạng thái Online nhưng điện áp đo được tại cực pack pin xấp xỉ bằng 0V hoặc < 50% điện áp định mức (contactor trên xe chưa đóng, cầu chì tổng xe bị đứt hoặc đứt cáp DC). | **Bo điều khiển trạm phát hiện liên động**:<br>- BMS đã online nhưng giá trị `bms.batt_voltage` đọc về từ bản tin BATT_ST1 nhỏ hơn 50% điện áp định mức cấu hình (`cfg_vmax_v * 0.5f`).<br>- Hàm `ev_bms_no_pack_voltage()` kích hoạt sau debounce 500ms. |
| **Controller** | Dừng khẩn cấp mềm (E-Stop) | **E020** | Dừng khẩn cấp (E-Stop) | **Dừng khẩn cấp (E-STOP)** | Chỉ do lệnh E-Stop từ PC/DWIN. Không giả lập GPIO khi phần cứng chưa có. | PC/DWIN command → `CHARGE_CTRL_FAULT_EMERGENCY_STOP` → `ALARM_CTRL_EMERGENCY_STOP` → `E020` → E-STOP; dispatch edge một lần. **Đã triển khai.** |
| **Controller** | Mất pha điện áp AC trạm | **E025** | Mất pha điện áp AC vào | **Dừng sạc an toàn (Stop Charge)**<br>Dừng sạc, ngắt contactor AC cấp nguồn chính và contactor DC để bảo vệ toàn bộ thiết bị. | Mạch giám sát nguồn AC của bo điều khiển trạm sạc phát hiện mất ít nhất 1 trong 3 pha lưới điện (L1, L2, L3) cấp vào trạm sạc (thời gian debounce > 1000ms). | **Bo điều khiển trạm ánh xạ cờ từ module sạc**:<br>- Dòng 331 `alarm.c` ghi rõ: `if (in->mod_alarm_or & CHG_LIB_ALARM_AC_PHASE_LOSS) in->ac_phase_loss = true;`<br>- Không tính toán nội suy điện áp, lấy thẳng từ cờ mất pha do module gửi lên qua debounce 1000ms. Latching = true. |
| **Controller** | Sụt áp nguồn AC đầu vào trạm | **E026** | Sụt áp nguồn AC đầu vào | **Dừng sạc an toàn (Stop Charge)**<br>Dừng sạc, ngắt contactor để bảo vệ trạm. | Điện áp pha/dây của nguồn AC 3 pha cấp vào trạm sạc bị sụt giảm đồng thời xuống dưới ngưỡng vận hành tối thiểu cho phép trong thời gian kéo dài (> 1000ms). | **Bo điều khiển trạm ánh xạ cờ từ module sạc**:<br>- Dòng 335 `alarm.c` ghi rõ: `if (in->mod_alarm_or & CHG_LIB_ALARM_AC_UNDER_VOLT) in->ac_undervolt = true;`<br>- Không tính toán nội suy điện áp, lấy thẳng từ cờ thấp áp do module gửi lên qua debounce 1000ms. Latching = true. |
| **Controller** | Mất tải đầu ra DC | **E023** | Mất tải đầu ra DC | **Dừng sạc an toàn (Stop Charge)**<br>Khóa lệnh dòng sạc, mở rơ-le DC ngay lập tức để chống phát sinh hồ quang nguy hiểm. | Dòng sạc DC đột ngột sụt giảm về 0A (hoặc < 15% dòng điều khiển) trong khi điện áp vẫn đang duy trì ở mức cao. Thường do người dùng giật rút súng sạc đột ngột (hot unplug) hoặc contactor xe bị nhả đột ngột. | **Bo điều khiển trạm tự phát hiện theo thuật toán tải**:<br>- Hàm `ev_dc_load_lost()` trong `alarm.c` so sánh:<br>Khi đang ở `RUNNING` và dòng sạc tụt xuống `< 15%` dòng đặt mục tiêu trong khi điện áp vẫn bám sát `> 98%` áp mục tiêu.<br>- Debounce: `ALARM_LOAD_LOST_MS = 800ms`. Latching = true. |
| **Controller** | Chưa thiết lập đầu ra DC | **E024** | Chưa thiết lập đầu ra DC | **Dừng sạc an toàn (Stop Charge)**<br>Hủy lệnh khởi động sạc, mở lại contactor sạc trạm, báo lỗi trên màn hình. | Sau khi rơ-le DC sạc của trạm được điều khiển đóng, dòng sạc thực tế không hình thành được trong khoảng thời gian timeout cho phép (5000ms). Thường do tiếp điểm contactor trạm bị kẹt/không tiếp xúc, hoặc mạch phía xe chưa khép kín. | **Bo điều khiển trạm tự đếm Timer sau đóng rơ-le**:<br>- Hàm `ev_dc_out_not_established()` trong `alarm.c`:<br>Đã phát lệnh đóng rơ-le quá 5000ms (`now - relay_close_since >= 5000ms`) nhưng dòng sạc qua cảm biến vẫn `< 2.0A` (`ALARM_I_LOAD_MIN_A`). Latching = true. |
| **Controller** | Không tìm thấy module sạc | **E027** | Không tìm thấy module sạc | **Từ chối sạc (Inhibit Charge)**<br>Khóa chức năng bắt đầu sạc, thông báo lỗi phần cứng trạm. | Bo điều khiển trung tâm quét trên đường truyền CAN nhưng không phát hiện được bất kỳ module sạc nào phản hồi online tại thời điểm khởi tạo hệ thống. | **Bo điều khiển trạm tự kiểm tra trước khi sạc**:<br>- Khi nhận lệnh sạc, `charge_controller.c` kiểm tra `mod_online_count == 0` hoặc driver chưa chọn.<br>- Bật cờ `CHARGE_CTRL_FAULT_NO_MODULE`. |
| **Controller** | Số lượng module không khớp | **E028** | Số lượng module không khớp | **Từ chối sạc (Inhibit Charge)** | Số module thực tế khác số module cấu hình. | `actual_module_count != configured_module_count` → `CHARGE_CTRL_FAULT_MODULE_COUNT_MISMATCH` → `ALARM_CTRL_MODULE_MISMATCH` → `E028`; theo logic controller hiện tại. **Đã triển khai/đã test.** |
| **Controller** | Cấu hình sạc không hợp lệ | **E029** | Cấu hình sạc không hợp lệ | **Từ chối sạc (Inhibit Charge)** | `ChargeCycleConfig_Set()` từ chối cấu hình theo validation thật của module config. | Caller/config owner nhận kết quả reject và chịu trách nhiệm mirror `CHARGE_CTRL_FAULT_INVALID_CONFIG` → `ALARM_CTRL_INVALID_CONFIG` → `E029`; không tạo bản sao validation trong alarm module. **Mô tả đã chuẩn hóa; cần xác nhận caller update nếu muốn phát E029 khi reject.** |
| **Jack cắm** | Quá nhiệt jack cắm sạc | **E031** | Bảo vệ quá nhiệt jack cắm | **Derating hoặc STOP theo controller** | Nhiệt độ jack vượt các ngưỡng cấu hình. | NTC/ADC → `temp_jack_degc` → cấu hình threshold/trip/delay/power-limit → `CHARGE_CTRL_FAULT_PROTECT_JACK_TEMP` → `ALARM_CTRL_JACK_OVER_TEMP` → `E031`; debounce theo controller. Không ghi cứng giá trị vào mapping. **Đã triển khai.** |
| **Jack cắm** | Bảo vệ quá áp jack cắm | **E030** | Bảo vệ chênh lệch điện áp module/BMS | **Dừng sạc an toàn (STOP)** | Chênh lệch điện áp giữa module và BMS vượt cấu hình. | `V_module - V_bms` → threshold/trip/delay cấu hình → `CHARGE_CTRL_FAULT_PROTECT_JACK_V` → `ALARM_CTRL_JACK_OVER_V` → `E030`; không mô tả là ADC jack. **Đã triển khai/đã test.** |

> W012/W013 là warning INFO độc lập. Warning không tự nâng cấp thành E-code theo thời gian; chỉ protection/fault bit tương ứng mới tạo E012/E013. HTML/Excel là generated artifacts từ bảng canonical này.
  
